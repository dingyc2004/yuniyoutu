# “鱼你有图”项目功能差距与修改路线

> 审查日期：2026-06-14  
> 审查目标：让当前 Vue3 + FastAPI 演示项目与《商业PPT逐页内容与中国市场调研.md》的产品叙事一致。  
> 当前边界：继续使用 Vue3 + Vite、FastAPI 和根目录 JSON 集合；不引入 Docker；新增集合必须同时增加 `.schema.json`。

## 一、审查结论

当前项目已经能够演示“地图找点、开始/结束记录、社区内容、教程、个人主页与会员报告”的基本链路，前端生产构建成功。

但当前代码更接近**界面完整的单用户演示 Demo**，尚未形成商业 PPT 中描述的“个人钓鱼档案 + 结构化社区 + 单级会员 + 社群活动 + 教程与装备服务”产品闭环。

最需要优先解决的不是增加更多页面，而是让已有核心链路中的数据真实、可持续、可关联：

1. 记录页必须真正录入鱼种、条数、重量、钓法和饵料。
2. 新增记录和帖子必须持久化，不能只存在内存或浏览器本地。
3. 个人主页必须从硬编码身份改为真实用户资料，并统一为一级会员。
4. 报告算法必须区分“使用频率”和“实际效率”，避免把常用钓法误称为最高效钓法。
5. 社区帖子必须与真实出钓记录关联，并实现位置模糊化。

## 二、当前已经实现的能力

### 1. 个人主页与会员报告

对应文件：`frontend/src/components/MineView.vue`

- 展示用户名称、偏好标签、总出钓、探索钓点和总重量。
- 聚合“我的记录、我的发布、收藏夹”。
- 支持查看单次记录详情，包括时间、时长、天气、鱼获、钓法、饵料和复盘。
- 根据记录在前端计算：
  - 总鱼获与总重量
  - 常钓鱼种
  - 高频钓点
  - 偏好时段
  - 常见温度区间
  - 常用钓法
- 支持生成会员报告弹窗，并导出、分享报告图片。

### 2. 出钓记录

对应文件：

- `frontend/src/components/RecordView.vue`
- `backend/app/schemas/record.py`
- `backend/app/api/routes_records.py`
- `backend/app/services/record_service.py`

已实现开竿计时、收竿汇总、位置选择、天气温度关联和记录 CRUD 接口。后端记录模型已包含鱼种、条数、重量、钓法、饵料、笔记与图片字段。

### 3. 社区与发布

对应文件：

- `frontend/src/components/CommunityView.vue`
- `frontend/src/components/PublishView.vue`
- `frontend/src/components/PostViewer.vue`
- `backend/app/api/routes_posts.py`

已实现社区频道、搜索、图文/视频发布界面、从出钓记录快速填充帖子、帖子详情、前端点赞/评论/收藏演示和基础帖子接口。

### 4. 地图、推荐与教程

- 已有 POI、天气、规则评分和推荐接口。
- 推荐采用规则模型排序，AI 主要负责解释，符合 PPT 中“不把 AI 当作核心壁垒”的表述。
- 教程页支持入门/进阶和图文/视频筛选。

## 三、与商业 PPT 的主要差距

| PPT 中承诺的能力 | 当前实现情况 | 主要问题 | 优先级 |
| --- | --- | --- | --- |
| 完整个人出钓档案 | 部分实现 | 记录汇总弹窗没有鱼种、条数、重量、钓法、饵料输入框；真实新增记录默认数据为空或为 0 | P0 |
| 数据长期沉淀 | 未真正实现 | 后端新增记录和帖子只写入内存；服务重启后丢失，前端主要依赖 `localStorage` | P0 |
| 单级会员 | 与叙事冲突 | `MineView.vue` 仍保留金竿、银竿、普通会员多档逻辑，且会员身份硬编码 | P0 |
| 个性化报告 | 演示实现 | 只有生涯汇总；没有月报、年报、报告历史和后端报告接口 | P0 |
| 效率分析 | 算法口径不准确 | “最高效钓法、最佳时段、最佳温度”实际按记录出现次数统计，不是按单位时间鱼获效率计算 | P0 |
| 真实用户资料 | 未实现 | 用户名、偏好、作者和 `demo_user` 均硬编码，没有用户集合、登录或资料编辑 | P0 |
| 结构化钓鱼社区 | 部分实现 | 帖子标签存在，但没有稳定关联 `record_id`；评论、点赞、关注和收藏没有后端持久化 | P1 |
| 私聊、群聊和同城社群 | 未实现 | 只有页面频道和提示消息，没有会话、消息、群成员等数据模型 | P1 |
| 活动组织与报名 | 未实现 | 没有活动、报名、签到和活动群数据集合与页面 | P1 |
| 精确钓点默认私密 | 部分实现 | 发布页只有私密/朋友/公开，没有“精确、模糊水域、完全隐藏”独立位置权限；后端也没有模糊化逻辑 | P1 |
| 湖钓/海钓场景模板 | 未实现 | 记录模型缺少潮汐、风浪、船次、船长、费用、安全确认等海钓字段 | P1 |
| 个性化教程推荐 | 未实现 | 教程是静态列表，没有学习进度、用户阶段或基于记录的推荐 | P2 |
| 装备实测与交易 | 未实现 | 帖子中有装备测评内容，但没有装备、商家、商品、订单或真实使用关联模型 | P2 |
| 钓鱼人生护照/空军博物馆 | 未实现 | 当前可以统计鱼种和空军，但没有对应产品页面、徽章或分享卡 | P2 |
| 数据质量与安全 | 基础不足 | 没有鉴权，任意请求可通过 `user_id` 查询或修改记录；没有内容审核与操作审计 | P1 |
| 自动化测试 | 未实现 | 前端没有 Vitest/Playwright，后端没有 pytest 测试 | P1 |

## 四、按代码模块的修改方式

### 模块 1：JSON 数据层与持久化

**涉及代码**

- `backend/app/data/json_store.py`
- `backend/app/services/record_service.py`
- `backend/app/api/routes_posts.py`
- 根目录 `data/*.json` 与对应 `.schema.json`

**应该改什么**

- 当前新增记录和帖子只写入内存，后端重启后丢失。
- 前端真实数据、浏览器本地数据和演示种子数据混合，个人报告可能统计到非用户数据。

**具体修改方式**

1. 在 `json_store.py` 新增：
   - `save_collection(collection_name, items)`：将集合写回 JSON。
   - `update_collection(collection_name, updater)`：集中处理读、修改、写回。
2. 写入时先生成同目录临时文件，再原子替换正式 JSON，避免文件损坏。
3. `record_service.py` 的创建、更新、删除操作完成后调用 `save_collection("records", RECORDS_STORAGE)`。
4. 将帖子内存操作从 `routes_posts.py` 移到独立 `post_service.py`，所有创建和互动操作统一持久化。
5. 从 `App.vue` 移除自动合并 `SEED_RECORDS` 的逻辑；演示数据应直接归属于明确的 demo 用户，真实用户报告只统计自己的记录。

**验收标准**

- 新增记录和帖子后重启后端，数据仍存在。
- 同一条记录不会因为后端、`localStorage` 和种子数据合并而重复。

### 模块 2：出钓记录链路

**涉及代码**

- `frontend/src/components/RecordView.vue`
- `frontend/src/components/RecordLocationDialog.vue`
- `backend/app/schemas/record.py`
- `backend/app/api/routes_records.py`
- `data/records.schema.json`

**应该改什么**

- `summaryForm` 已有鱼种、条数、重量、钓法和饵料字段，但汇总弹窗没有输入控件，新记录通常以空值或 `0` 保存。
- 当前模型无法明确表示空军、最大单尾、装备和湖钓/海钓场景。

**具体修改方式**

1. 在收竿汇总弹窗补充鱼种、条数、总重量、最大单尾、钓法、饵料/拟饵、装备和复盘输入。
2. 当 `fish_count === 0` 时自动设置 `is_blank_trip = true`，允许用户补充空军原因。
3. 新增 `record_template`：
   - `lake`：水情、钓法、饵料。
   - `sea`：潮汐、风浪、船次、船长、费用和返航确认。
4. 将图片占位数量改为真实图片 URL 或本地对象 URL；保存时写入 `images`。
5. 后端 Pydantic 模型和 `records.schema.json` 同步增加字段并校验。

**建议字段**

```text
is_blank_trip
blank_reason
max_single_weight
equipment_ids
water_type
record_template
tide
wave_level
boat_name
captain_name
trip_cost
privacy_level
```

**验收标准**

- 完成一次新记录后，个人主页能正确显示鱼种、条数、重量、钓法和复盘。
- 空军记录不会被误判为数据缺失。

### 模块 3：用户资料、个人主页与单级会员

**涉及代码**

- `frontend/src/components/MineView.vue`
- `frontend/src/App.vue`
- 新增 `backend/app/schemas/user.py`
- 新增 `backend/app/api/routes_users.py`
- 新增 `backend/app/services/user_service.py`
- 新增 `data/users.json`、`data/users.schema.json`
- 新增 `data/memberships.json`、`data/memberships.schema.json`

**应该改什么**

- “武汉钓友 008”、偏好、“金竿会员”和有效期均为硬编码。
- 当前存在金竿、银竿、普通会员多档逻辑，与 PPT 的单级会员冲突。

**具体修改方式**

1. 建立用户集合，保存昵称、头像、城市、经验等级、偏好钓法和偏好鱼种。
2. 建立会员集合，只保留 `active/inactive/expired` 状态，不再设置会员等级。
3. 新增 `GET /api/users/{user_id}` 和 `GET /api/users/{user_id}/membership`。
4. `App.vue` 加载当前 demo 用户后，将 `user` 和 `membership` 作为 props 传给 `MineView.vue`、`PublishView.vue`。
5. `MineView.vue`：
   - 非会员展示完整档案、基础统计和升级入口。
   - 会员展示统一“鱼你有图会员”和报告入口。
   - 删除 `memberTier` 及金竿/银竿样式分支。

**验收标准**

- 更换 `user_id` 后，个人主页昵称、记录和会员状态同步变化。
- 非会员可以完整记录和使用社区，只有升级报告能力受限。

### 模块 4：个人统计、会员报告与品牌分享卡

**涉及代码**

- `frontend/src/components/MineView.vue`
- 建议新增 `frontend/src/components/MemberReportView.vue`
- 建议新增 `frontend/src/components/FishingPassportCard.vue`
- 新增 `backend/app/services/report_service.py`
- 新增 `backend/app/api/routes_reports.py`
- 新增 `data/report_snapshots.json`、`data/report_snapshots.schema.json`

**应该改什么**

- 当前“最高效钓法、最佳时段、最佳温度”实际按出现次数统计，属于偏好而非效率。
- 报告只在前端临时生成，没有月报、年报、生涯报告和历史快照。
- “今日智能推荐”部分存在硬编码。

**具体修改方式**

1. 将报告计算移入 `report_service.py`，确保前端和导出图片使用同一结果。
2. 将指标拆分：
   - 偏好：最常用钓法、最常出钓时段、最常去水域。
   - 效率：单位小时鱼获、单位小时重量、空军率、不同条件成功率。
3. 样本少于 5 次时显示“数据积累中”，不输出伪精确结论。
4. 增加报告周期参数 `month/year/lifetime`，生成后写入 `report_snapshots`。
5. 将当前 Canvas 导出逻辑拆到独立分享卡组件，并增加“钓鱼人生护照”和“空军博物馆”卡片模板。
6. 推荐内容调用现有推荐接口，删除固定钓点和固定时间窗口。

**建议接口**

```text
GET /api/users/{user_id}/profile-summary
GET /api/users/{user_id}/reports?period=month|year|lifetime
POST /api/users/{user_id}/reports
GET /api/reports/{report_id}
```

**验收标准**

- 同一组记录在前端页面和导出图片中的统计一致。
- 月报、年报、生涯报告可分别生成并回看。

### 模块 5：结构化社区、互动与位置隐私

**涉及代码**

- `frontend/src/components/CommunityView.vue`
- `frontend/src/components/PublishView.vue`
- `frontend/src/components/PostViewer.vue`
- `frontend/src/services/api.js`
- `backend/app/schemas/post.py`
- `backend/app/api/routes_posts.py`
- `data/posts.schema.json`
- 建议新增评论、互动和关注集合

**应该改什么**

- 前端可以从记录填充帖子，但后端帖子没有稳定关联 `record_id`。
- 点赞、评论、收藏和关注主要是前端演示，没有后端持久化。
- 内容可见性与位置可见性混在一起，公开帖子可能泄露精确坐标。

**具体修改方式**

1. 帖子增加：

```text
user_id
record_id
content_type
location_visibility
location_area_name
equipment_ids
```

2. 发布时让用户分别选择：
   - 内容权限：公开、朋友、私密。
   - 位置权限：精确位置、水域模糊、仅城市、完全隐藏。
3. 后端返回公开帖子前执行位置脱敏；不能只在前端隐藏坐标。
4. 新增评论、点赞/收藏、关注接口，并将对应数据持久化。
5. 社区频道从前端字符串筛选升级为按 `content_type`、城市、钓法和鱼种查询。

**建议接口**

```text
POST /api/posts
POST /api/posts/{post_id}/comments
POST /api/posts/{post_id}/reactions
POST /api/users/{user_id}/follow
GET /api/feed?channel=&city=&method=&species=
```

**验收标准**

- 从一条真实记录可以一键生成帖子，并能回到原记录。
- 公开帖子无法获得未授权的精确经纬度。

### 模块 6：社群活动、私聊与群聊

**涉及代码**

- 建议新增 `frontend/src/components/EventsView.vue`
- 建议新增 `frontend/src/components/GroupView.vue`
- 建议新增 `frontend/src/components/ChatView.vue`
- 新增对应 schemas、routes、services 和 JSON 集合

**应该改什么**

- PPT 已将同城约钓、比赛、拼船和活动群作为核心能力，但当前没有对应代码。

**具体修改方式**

1. 先实现活动最小闭环：活动列表、详情、报名、取消报名、签到。
2. 活动字段包含组织者、城市、水域、时间、人数、费用、安全要求和关联群组。
3. 报名成功后自动加入活动群。
4. 群聊 MVP 使用轮询接口即可，暂不必比赛前引入 WebSocket。
5. 海钓活动补充船次、船长、拼船人数和返航确认。

**建议集合**

```text
events
event_registrations
groups
group_members
messages
```

**验收标准**

- 用户可以从社区活动卡进入活动、报名并进入活动群。
- 活动结束后能生成一条个人活动履历或出钓记录。

### 模块 7：教程、装备与商家服务

**涉及代码**

- `frontend/src/components/TutorialsView.vue`
- 建议新增装备与商家组件
- 新增教程进度、装备、商家和订单相关后端模块

**应该改什么**

- 教程目前是静态内容列表。
- 装备只存在于帖子文本中，没有真实使用记录和商业链路。

**具体修改方式**

1. 增加 `learning_progress`，记录用户完成、收藏和练习状态。
2. 报告根据用户记录短板推荐教程，而不是展示通用列表。
3. 记录增加 `equipment_ids`，装备详情展示真实使用次数、鱼种和代表战绩。
4. 先实现认证商家、商品链接和体验活动；订单与支付放在商业验证阶段。

**验收标准**

- 用户报告可以跳转到一条与其短板相关的教程。
- 装备详情能够展示关联的真实出钓记录。

### 模块 8：接口整合、测试、安全与文档

**涉及代码**

- `frontend/src/services/api.js`
- `frontend/src/App.vue`
- `backend/app/main.py`
- 后端各路由模块
- `README.md`
- 新增测试目录

**应该改什么**

- API 调用、种子数据回退和本地状态分散在组件及 `App.vue`。
- 当前没有鉴权、自动化测试，README 与实际实现不一致。
- 主 JS 包约 `1.1 MB`，存在首次加载压力。

**具体修改方式**

1. 按领域拆分前端 API：records、users、reports、posts、events。
2. 将加载、错误、离线状态统一处理，避免组件静默使用假数据。
3. 增加基础鉴权；后端从登录身份获取用户 ID，不允许客户端任意修改他人数据。
4. 增加：
   - 后端 pytest：记录 CRUD、报告统计、位置脱敏。
   - 前端 Vitest：记录表单和报告计算展示。
   - Playwright：开竿到分享的完整演示链路。
5. 对地图、报告、帖子详情使用动态加载。
6. 更新 README，统一 conda 环境为 `fish`，明确真实接口和 mock 能力。

**验收标准**

- 自动化测试覆盖核心演示链路。
- API 失败时用户能看到明确状态，不会悄悄混入种子数据。

## 五、推荐修改路线与依赖顺序

### 路线 1：先让数据真实且不会丢

先修改：

1. **模块 1：JSON 数据层与持久化**
2. **模块 2：出钓记录链路**

原因：个人主页、报告、社区和商业能力都依赖高质量、可持久化的出钓记录。若记录仍为空值或重启即丢，后续功能都只是界面演示。

完成标志：

`开竿 → 填写真实鱼获 → 保存 → 重启后端 → 记录仍存在`

### 路线 2：再形成个人档案与会员价值

接着修改：

1. **模块 3：用户资料、个人主页与单级会员**
2. **模块 4：个人统计、会员报告与品牌分享卡**

原因：这是 PPT 的核心差异，也是最直接的会员付费理由。

完成标志：

`用户记录 → 个人主页汇总 → 生成月报/年报/人生护照 → 保存分享`

### 路线 3：再把记录转化为可信社区内容

接着修改：

1. **模块 5：结构化社区、互动与位置隐私**
2. **模块 6：社群活动、私聊与群聊**

原因：社区必须建立在真实记录和隐私保护之上，不能先做聊天页面再补数据关系。

完成标志：

`真实记录 → 一键发布 → 位置脱敏 → 社区互动 → 报名活动/加入群`

### 路线 4：最后扩展服务与商业化

最后修改：

1. **模块 7：教程、装备与商家服务**
2. 活动收费、会员支付、订单和品牌合作。

原因：只有在记录、用户关系和活动场景形成后，教程推荐和装备交易才不会变成普通广告商城。

完成标志：

`报告识别短板 → 推荐教程/活动/装备 → 产生服务收入 → 推动下一次出钓`

### 全程并行：工程质量

**模块 8** 不应等到最后统一补，应在每条路线完成时同步增加接口测试、前端关键逻辑测试和 README 更新。

## 六、必须新增或调整的数据集合

| 集合 | 用途 | 建议加入路线 |
| --- | --- | --- |
| `users` | 用户资料与偏好 | 路线 2 |
| `memberships` | 单级会员状态与到期时间 | 路线 2 |
| `report_snapshots` | 月报、年报、生涯报告历史 | 路线 2 |
| `comments`、`reactions`、`follows` | 社区真实互动 | 路线 3 |
| `events`、`event_registrations` | 活动与报名 | 路线 3 |
| `groups`、`group_members`、`messages` | 社群与聊天 | 路线 3 |
| `learning_progress` | 教程学习与成长路径 | 路线 4 |
| `equipment`、`merchants` | 装备履历与商家服务 | 路线 4 |

每个新增集合必须同步创建同名 `.schema.json`。

## 七、最终优先级

如果只完成最核心的一条路线，应依次完成：

1. 补齐记录页鱼获输入并持久化。
2. 移除硬编码用户和多级会员，建立单级会员个人主页。
3. 修正报告算法，生成可分享的会员报告和钓鱼人生护照。
4. 将帖子与真实记录关联，并在后端执行位置脱敏。
5. 完成“记录 → 个人主页 → 报告 → 分享社区”的自动化演示测试。

这条路线完成后，代码才能真正支撑项目的核心主张：**先记录，再连接，后服务。**

## 八、当前验证结果

- 前端 `npm run build`：成功。
- 后端静态编译：未完成，因为当前机器未找到名为 `fish` 的 conda 环境。
- 当前未发现前后端自动化测试。
