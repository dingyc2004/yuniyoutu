\section{功能响应表}

本章对照课程实习技术评分表和本期交付范围，逐项说明功能响应情况。响应状态统一为“已实现、部分实现、待补充、后续规划”四类；每条响应均给出实现说明和验证方式，避免只写“支持”或“满足”而缺少可核查依据。

\begin{table}[H]
  \centering
  \caption{功能响应状态说明}
  \label{tab:response-status}
  \begin{tabularx}{0.82\textwidth}{p{2.8cm}|X}
    \toprule
    \textbf{状态} & \textbf{含义} \\
    \midrule
    已实现 & 本期代码和页面已提供可演示、可验证的完整闭环 \\
    \hline
    部分实现 & 已有页面、接口或数据基础，但仍存在鉴权、持久化、真实服务接入或体验完善空间 \\
    \hline
    待补充 & 本期需要补充文档、配置模板、测试证据或少量工程化材料 \\
    \hline
    后续规划 & 不作为本期验收承诺，仅说明正式工程化阶段的扩展路线 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{longtable}{|p{0.6cm}|p{3.0cm}|p{1.7cm}|p{5.4cm}|p{3.7cm}|}
  \caption{功能需求响应表}
  \label{tab:func-response}\\
  \toprule
  \textbf{\#} & \textbf{需求项} & \textbf{状态} & \textbf{实现说明} & \textbf{验证方式} \\
  \midrule
  \endfirsthead

  \multicolumn{5}{c}{\tablename~\thetable\quad（续）}\\
  \toprule
  \textbf{\#} & \textbf{需求项} & \textbf{状态} & \textbf{实现说明} & \textbf{验证方式} \\
  \midrule
  \endhead

  \bottomrule
  \multicolumn{5}{r}{\footnotesize 续下页}\\
  \endfoot

  \bottomrule
  \endlastfoot

  1.1 & 项目背景、建设目标、用户场景 & 已实现 & 第 1 章和第 2 章说明垂钓用户“去哪钓、能不能钓、有没有鱼、如何分享、如何学习”的场景，并明确课程 MVP 建设目标 & 检查第 1、2 章；确认本期目标与后续规划分开描述 \\
  \hline
  1.2 & 本期 MVP 交付范围 & 已实现 & 本期聚焦地图找点、POI 展示、规则推荐与 AI 解释、鱼获/空军发布、社区鱼获流和教程展示；活动、订单、群聊和报告作为 Demo 能力展示扩展方向 & 检查第 1 章范围边界、第 18 章后续规划；确认未把真实支付、正式监管合作、商户后台写成当前交付 \\
  \hline
  2.1 & 地图首页与 POI 展示 & 已实现 & \texttt{HomeView.vue} 与 \texttt{MapPanel.vue} 集成高德 JS API，POI 数据由 \texttt{/api/pois} 从 \texttt{data/pois.json} 返回，页面展示地图标记、钓点卡片、天气摘要和搜索入口 & 打开首页地图；调用 \texttt{GET /api/pois}；检查返回字段 \texttt{id/name/lat/lng/fish\_species/score} 与页面标记一致 \\
  \hline
  2.2 & 钓点详情与风险提示 & 已实现 & 点击 POI 后展示名称、类型、距离、鱼种、设施、评分分项、推荐理由和风险提示，详情数据来自 \texttt{/api/pois/\{poi\_id\}} & 打开钓点详情；调用详情接口；检查 \texttt{risk\_flags}、\texttt{facilities}、\texttt{score\_breakdown} 等字段是否渲染 \\
  \hline
  2.3 & 规则推荐排序 & 已实现 & \texttt{/api/recommendations} 基于候选 POI、天气、距离、鱼种、设施、热度和风险标签计算六维评分，AI 不参与排序 & 调用推荐接口；检查 \texttt{score}、\texttt{breakdown}、\texttt{reasons}、\texttt{warnings}；禁钓或高风险点位应降权或提示 \\
  \hline
  2.4 & AI 推荐解释 & 部分实现 & \texttt{/api/ai/fishing-advice} 接收规则推荐结果和用户意图，返回自然语言解释；生产环境配置 DeepSeek Key 时调用真实服务，未配置时返回 mock 解释 & 分别在无 Key 和有 Key 环境调用接口；检查输出 \texttt{summary/top\_recommendations/tips}，确认解释不改变推荐排序 \\
  \hline
  2.5 & 鱼获/空军记录 & 已实现 & \texttt{RecordView.vue} 支持开竿计时、收竿汇总、鱼种、数量、重量、钓法、饵料和空军标记；后端 \texttt{/api/records} 写入 \texttt{data/records.json} & 完成一次鱼获记录和一次空军记录；重启后端后调用 \texttt{GET /api/records}，确认记录仍存在 \\
  \hline
  2.6 & 社区鱼获流与发布 & 已实现 & \texttt{CommunityView.vue} 展示社区流，\texttt{PublishView.vue} 支持图文发布、关联钓点、关联记录和位置权限；后端 \texttt{/api/posts}、\texttt{/api/feed} 提供数据 & 发布一条关联 \texttt{record\_id} 的帖子；刷新社区流；检查帖子标题、图片、钓点和位置权限展示 \\
  \hline
  2.7 & 评论、点赞、收藏、关注与私信 & 部分实现 & \texttt{routes\_community.py} 提供评论、互动、关注、好友、私信和通知接口，支撑 Demo 级社交互动；正式登录鉴权和内容审核为后续增强 & 调用 \texttt{POST /api/posts/\{id\}/comments}、\texttt{POST /api/posts/\{id\}/reactions}、\texttt{GET /api/direct-messages/\{user\}/\{peer\}}；检查 JSON 集合写入结果 \\
  \hline
  2.8 & 教程展示与学习进度 & 部分实现 & 教程列表由 \texttt{/api/tutorials} 返回；\texttt{/api/tutorials/\{tutorial\_id\}/progress} 支持学习状态更新；个性化教程推荐仍以规则和报告结果为主 & 进入教程页切换分类；更新某教程进度；调用 \texttt{GET /api/users/\{user\_id\}/learning-progress} 检查状态 \\
  \hline
  2.9 & 个人主页与会员报告 & 部分实现 & \texttt{MineView.vue} 展示个人统计和报告弹窗；\texttt{/api/users/\{user\_id\}/profile-summary} 与 \texttt{/api/users/\{user\_id\}/reports} 提供统计与报告快照；完整会员权益体系为后续规划 & 新增多条记录后刷新个人主页；调用 profile-summary 和 reports 接口；检查总次数、鱼种、重量、偏好时段等统计 \\
  \hline
  2.10 & 活动报名、签到与活动群 & 部分实现 & \texttt{routes\_events.py} 提供活动列表、报名、取消报名、签到、从活动生成记录、群消息和装备列表接口，作为本期服务扩展 Demo & 打开服务页；调用 \texttt{GET /api/events}、\texttt{POST /api/events/\{id\}/register}、\texttt{POST /api/events/\{id\}/checkin}；检查报名状态和消息列表 \\
  \hline
  2.11 & 装备订单演示 & 部分实现 & \texttt{routes\_orders.py} 提供订单创建、模拟支付、取消和用户订单列表；当前不接入真实支付、退款和商家结算 & 调用 \texttt{POST /api/orders}、\texttt{POST /api/orders/\{id\}/pay}、\texttt{GET /api/users/\{id\}/orders}；检查订单状态变化 \\
  \hline
  3.1 & 前后端分离架构 & 已实现 & Vue 3 + Vite 前端通过集中 \texttt{api.js} 调用 FastAPI；Vite 代理 \texttt{/api} 到后端 8000 端口 & 检查 \texttt{vite.config.js} 代理配置、\texttt{backend/app/main.py} 路由注册和浏览器网络请求 \\
  \hline
  3.2 & 后端三层架构 & 已实现 & 后端按路由层、服务层、JSON 数据层组织；\texttt{json\_store.py} 采用临时文件和原子替换策略写入集合 & 检查 \texttt{app/api/}、\texttt{app/services/}、\texttt{app/data/json\_store.py}；新增记录后确认 JSON 文件更新 \\
  \hline
  3.3 & 数据模型与 Schema & 已实现 & 根目录 \texttt{data/} 下各 JSON 集合配套同名 \texttt{.schema.json}，覆盖 POI、记录、帖子、教程、活动、订单、消息、报告等集合 & 检查 \texttt{data/} 目录；抽查 \texttt{pois.schema.json}、\texttt{records.schema.json}、\texttt{posts.schema.json} 与接口字段是否对应 \\
  \hline
  3.4 & 环境变量与密钥管理 & 待补充 & 后端通过 \texttt{config.py} 从 \texttt{.env} 读取高德和 DeepSeek 配置；文档应提交不含真实密钥的环境变量清单或模板，真实 Key 不进入代码仓库 & 检查前端源码无高德 Web Service Key 和 DeepSeek Key；补充部署材料中的环境变量模板 \\
  \hline
  4.1 & 系统架构图、业务流程图、数据流图 & 已实现 & 第 4 章、第 5 章引用系统架构图、核心业务流程图和数据流图，正文对图示流程进行说明 & 检查图题、编号、正文引用和图中组件是否与技术栈一致 \\
  \hline
  4.2 & 推荐模型流程图 & 待补充 & 第 9 章新增推荐流程图占位，说明“候选 POI → 特征计算 → 规则评分 → 风险校验 → AI 解释 → 前端展示”的路径 & 根据 \texttt{figures/recommendation\_flow.md} 绘制同名图片后，检查第 9 章引用是否正常 \\
  \hline
  5.1 & 测试与验收方法 & 已实现 & 第 14 章按功能、接口、数据、性能、安全、兼容性和验收阶段组织测试，并新增核心验收检查表 & 对照验收表逐项演示；每项应能回答页面、接口、字段、通过标准和失败兜底 \\
  \hline
  5.2 & 实施计划和团队分工 & 已实现 & 第 16 章给出 120 天实施阶段、阶段成果和项目经理/产品/前端/后端/测试/文档等角色分工 & 检查阶段任务、输出物和责任角色；确认不把后续工程化能力写成本期必须完成 \\
  \hline
  5.3 & 风险分析和后续扩展 & 已实现 & 第 17 章说明数据、隐私、第三方、推荐 AI、持久化、鉴权、内容治理、范围蔓延和验收表达风险；第 18 章说明后续扩展路线 & 检查风险项是否有触发条件、应对措施和验收证据；检查后续规划未误写为当前交付 \\
\end{longtable}
